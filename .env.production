# Production env for the React build on GitHub Pages.
# Set this to the deployed backend URL (Render / Railway / Fly / etc.).
# Must NOT have a trailing slash.
REACT_APP_API_URL=https://upi-donation-server.onrender.com
